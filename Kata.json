{
  "download": {},
  "call": {
    "update": "julia --project --eval 'using Pkg; Pkg.update(); Pkg.gc()'",
    "run": "julia --project --eval 'using Pkg; Pkg.test()'",
    "build": "rm -fr build && julia --project deps/build.jl app tarball"
  }
}
